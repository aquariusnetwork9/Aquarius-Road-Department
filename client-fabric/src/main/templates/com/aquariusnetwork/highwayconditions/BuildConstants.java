package ${maven_group};

// Filled in from gradle.properties before compilation.
public class BuildConstants {

    public static final String VERSION = "${version}";

    public static final String MC_VERSION = "${mc_version}";
}
