# Fabric properties (https://fabricmc.net/develop) -- this branch is a release-only port: same
# local-hazard-alert/addon-API/Baritone-auto-avoid featureset as the mod's current (main, 1.21.11)
# target, rebuilt against 1.21.8 so every previously-published MC version gets the new feature
# too. No source changes needed here -- HudElementRegistry/ClickEvent.OpenUrl are both already
# correct at 1.21.8 (see client-fabric/README.md's version table). Not meant to be merged to
# main; exists only so GitHub Actions has something to build+publish this exact version from.
minecraft_version=1.21.8
yarn_mappings=1.21.8+build.1
loader_version=0.19.3
fabric_version=0.136.1+1.21.8

# Mod properties
mod_version=0.2.0
maven_group=com.aquariusnetwork.highwayconditions
archives_base_name=ard

org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.caching=true
