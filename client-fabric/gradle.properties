# Fabric properties (https://fabricmc.net/develop) -- targets the same MC version
# plugin-aquarius/plugin-zenith already build against, so the whole product line stays on one
# Minecraft version. Roadmap: bring this forward through later versions over time (see
# client-fabric/README.md); this is intentionally NOT the newest available MC version.
minecraft_version=1.21.4
yarn_mappings=1.21.4+build.8
loader_version=0.19.3
fabric_version=0.119.4+1.21.4

# Mod properties
mod_version=0.1.0
maven_group=com.aquariusnetwork.highwayconditions
archives_base_name=ard

org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.caching=true
