# Fabric properties (https://fabricmc.net/develop) -- this branch is a release-only port: same
# local-hazard-alert/addon-API/Baritone-auto-avoid featureset as the mod's current (main, 1.21.11)
# target, rebuilt against 1.21.5. Unlike the 1.21.8/1.21.10 ports, this DOES need a source change
# -- HudElementRegistry doesn't exist yet at 1.21.5, see HighwayConditionsFabricClient's own
# registration call in this branch. ClickEvent.OpenUrl(URI) is already correct here (that shape
# has been current since ~1.21.5). Not meant to be merged to main; exists only so GitHub Actions
# has something to build+publish this exact version from.
minecraft_version=1.21.5
yarn_mappings=1.21.5+build.1
loader_version=0.19.3
fabric_version=0.128.2+1.21.5

# Mod properties
mod_version=0.2.0
maven_group=com.aquariusnetwork.highwayconditions
archives_base_name=ard

org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.caching=true
